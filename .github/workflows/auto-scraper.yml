name: Auto Aggregate Only

on:
  schedule:
    # 每天執行兩次統計 (UTC時間 02:00 和 14:00)
    - cron: '0 2,14 * * *'
  workflow_dispatch:

jobs:
  run-aggregator:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Create Firebase Key File
        run: |
          echo '${{ secrets.FIREBASE_KEY_JSON }}' > firebase-key.json

      - name: Build
        working-directory: ./stock-fetcher
        run: dotnet build
        
      - name: Install Playwright Browsers
        run: |
          # 假設您是在專案根目錄下運行，且有安裝 Microsoft.Playwright.CLI 工具
          dotnet tool restore
          dotnet playwright install chromium

      - name: Run Aggregation Only
        working-directory: ./stock-fetcher
        run: dotnet run -- aggregate